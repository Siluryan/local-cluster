apiVersion: v1
kind: Service
metadata:
  name: {{ include "crm.fullname" . }}-postgres
spec:
  selector:
    app.kubernetes.io/name: {{ include "crm.name" . }}
    app.kubernetes.io/component: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
